# PixiJS v8 클래스 구조 가이드

> PixiJS v8.15.0 기준으로 작성된 클래스 구조 분석 문서입니다.

## 목차

1. [Sprite vs Graphics vs Mesh](#1-sprite-vs-graphics-vs-mesh)
2. [전체 아키텍처 개요](#2-전체-아키텍처-개요)
3. [핵심 클래스 상속 계층도](#3-핵심-클래스-상속-계층도)
4. [주요 클래스별 상세 구조](#4-주요-클래스별-상세-구조)
5. [옵션 인터페이스 연결 관계](#5-옵션-인터페이스-연결-관계)
6. [초기화 흐름](#6-초기화-흐름)

---

## 1. Sprite vs Graphics vs Mesh

세 클래스 모두 `ViewContainer`를 상속하지만 **렌더링 방식과 용도가 완전히 다릅니다**.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        ViewContainer (추상)                              │
│                              │                                          │
│         ┌────────────────────┼────────────────────┐                     │
│         ▼                    ▼                    ▼                     │
│  ┌─────────────┐     ┌─────────────┐      ┌─────────────┐              │
│  │   Sprite    │     │  Graphics   │      │    Mesh     │              │
│  │ ─────────── │     │ ─────────── │      │ ─────────── │              │
│  │ 래스터 이미지│     │ 벡터 그래픽 │      │ 정점 기반   │              │
│  │ (비트맵)    │     │ (실시간 생성)│      │ (GPU 직접)  │              │
│  └─────────────┘     └─────────────┘      └─────────────┘              │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.1 Sprite - 비트맵 이미지 표시

```typescript
const sprite = new Sprite(Texture.from('character.png'));
```

| 특징 | 설명 |
|------|------|
| **데이터 소스** | Texture (이미지, 비디오 등) |
| **렌더링 방식** | GPU에서 텍스처를 사각형에 매핑 |
| **성능** | ⭐⭐⭐⭐⭐ 가장 빠름 (배칭 최적화) |
| **유연성** | 낮음 - 이미지 그대로만 표시 |

**사용 사례:** 캐릭터, 배경, UI 아이콘, 스프라이트 시트 애니메이션

### 1.2 Graphics - 벡터 그래픽 (실시간 생성)

```typescript
const graphics = new Graphics()
    .circle(100, 100, 50)
    .fill({ color: 0xff0000 })
    .rect(0, 0, 200, 100)
    .stroke({ width: 2, color: 0x00ff00 });
```

| 특징 | 설명 |
|------|------|
| **데이터 소스** | GraphicsContext (드로잉 명령) |
| **렌더링 방식** | CPU에서 경로 → GPU에서 삼각형으로 변환 |
| **성능** | ⭐⭐⭐ 중간 (복잡도에 따라 다름) |
| **유연성** | 높음 - 어떤 도형이든 그릴 수 있음 |

**사용 사례:** 동적 도형 (HP바, 차트), 디버그 시각화, 마스크 생성, SVG 렌더링

### 1.3 Mesh - 정점(Vertex) 기반 렌더링

```typescript
const geometry = new MeshGeometry({
    positions: new Float32Array([0,0, 100,0, 100,100, 0,100]),
    uvs: new Float32Array([0,0, 1,0, 1,1, 0,1]),
    indices: new Uint32Array([0,1,2, 0,2,3])
});
const mesh = new Mesh({ geometry, texture });
```

| 특징 | 설명 |
|------|------|
| **데이터 소스** | Geometry (정점, UV, 인덱스) + Texture |
| **렌더링 방식** | 정점 데이터를 GPU에 직접 전달 |
| **성능** | ⭐⭐⭐⭐ 빠름 (정점 수에 따라) |
| **유연성** | 매우 높음 - 정점별 변형 가능 |

**사용 사례:** 왜곡 효과, Rope/Ribbon 효과, 2.5D 효과, 커스텀 셰이더

### 1.4 렌더링 파이프라인 비교

```
Sprite:     [Texture] ──────────────────────────────────▶ [GPU]
            이미지 그대로                              빠름!

Graphics:   [Drawing API] ──▶ [Triangulate] ──▶ [Vertices] ──▶ [GPU]
            rect(), circle()    CPU에서 변환     정점 생성   렌더링

Mesh:       [Vertices + UVs] ───────────────────────────▶ [GPU]
            직접 정점 제어                             유연함!
```

### 1.5 선택 가이드

| 요구사항 | 선택 |
|----------|------|
| 이미지를 표시하고 싶다 | **Sprite** |
| 도형을 코드로 그리고 싶다 | **Graphics** |
| 텍스처를 왜곡/변형하고 싶다 | **Mesh** |
| 마스크를 만들고 싶다 | Graphics (또는 Sprite) |
| 밧줄/리본 효과 | **MeshRope** |
| 9-slice 스케일링 | **NineSliceSprite** |

### 1.6 Mixin 패턴 이해하기

PixiJS는 **Mixin 패턴**을 적극 활용합니다:

```typescript
// Container에 여러 기능을 Mixin으로 주입
definedMixins(Container, [
    cacheAsTextureMixin,      // 텍스처 캐싱 기능
    childrenHelperMixin,      // 자식 관리 헬퍼
    collectRenderablesMixin,  // 렌더링 수집
    effectsMixin,             // 이펙트 관리
    findMixin,                // 요소 찾기
    // ...
]);
```

**Mixin이란?**
- 클래스에 기능을 "섞어 넣는" 패턴
- 다중 상속을 우회하면서 여러 기능 조합 가능

---

## 2. 전체 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────────┐
│                        PixiJS v8 아키텍처                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                      Application                              │  │
│  │  ┌─────────────────┐    ┌────────────────────────────────┐   │  │
│  │  │     stage       │    │          renderer              │   │  │
│  │  │  (Container)    │    │  (WebGLRenderer/WebGPURenderer)│   │  │
│  │  └────────┬────────┘    └────────────────────────────────┘   │  │
│  └───────────┼───────────────────────────────────────────────────┘  │
│              │                                                      │
│              ▼                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Scene Graph (장면 그래프)                   │  │
│  │                                                               │  │
│  │    Container ─┬── Container ─┬── Sprite                      │  │
│  │    (stage)    │              ├── Graphics                    │  │
│  │               │              └── Text                        │  │
│  │               │                                              │  │
│  │               └── Container ─┬── Sprite                      │  │
│  │                              └── Mesh                        │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                      Rendering System                         │  │
│  │                                                               │  │
│  │  ┌────────────┐  ┌────────────┐  ┌─────────────────────────┐ │  │
│  │  │  Texture   │  │TextureSource│ │   Shader/Pipeline       │ │  │
│  │  └────────────┘  └────────────┘  └─────────────────────────┘ │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.1 프로젝트 디렉토리 구조

```
src/
├── app/                    # Application 클래스
├── scene/                  # Scene Graph 관련 클래스들
│   ├── container/          # Container 기본 클래스
│   ├── sprite/             # Sprite 클래스
│   ├── graphics/           # Graphics (벡터 그래픽)
│   ├── text/               # Text 렌더링
│   ├── text-bitmap/        # BitmapText
│   ├── text-html/          # HTMLText
│   ├── mesh/               # Mesh (3D-like)
│   ├── sprite-tiling/      # TilingSprite
│   ├── sprite-nine-slice/  # NineSliceSprite
│   └── particle-container/ # ParticleContainer
├── rendering/              # 렌더링 시스템
│   └── renderers/
│       ├── gl/             # WebGL 렌더러
│       ├── gpu/            # WebGPU 렌더러
│       └── shared/         # 공유 렌더링 로직
├── assets/                 # 에셋 로딩 시스템
├── events/                 # 이벤트 시스템
├── maths/                  # 수학 유틸리티 (Matrix, Point, Rectangle)
├── filters/                # 필터 효과
├── ticker/                 # 애니메이션 틱 관리
└── utils/                  # 유틸리티 함수들
```

---

## 3. 핵심 클래스 상속 계층도

### 3.1 Display Object 계층

```
                            EventEmitter
                                 │
                                 ▼
    ┌────────────────────────────────────────────────────────────┐
    │                        Container                           │
    │  - children 관리 (addChild, removeChild)                   │
    │  - transform (position, rotation, scale, skew, pivot)      │
    │  - 렌더링 속성 (alpha, visible, tint, blendMode)           │
    │  - 이벤트 처리 (EventEmitter 상속)                          │
    └────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
    ┌────────────────────────────────────────────────────────────┐
    │                    ViewContainer (추상)                     │
    │  - bounds 관리                                              │
    │  - GPU 데이터 관리                                          │
    │  - roundPixels 지원                                         │
    │  - containsPoint (히트 테스트)                              │
    └────────────────────────────────────────────────────────────┘
                                 │
            ┌────────────────────┼────────────────────┐
            ▼                    ▼                    ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐
    │    Sprite    │    │   Graphics   │    │   Mesh (추상)    │
    │  - texture   │    │  - context   │    │  - geometry      │
    │  - anchor    │    │  - 벡터 API  │    │  - shader        │
    └──────────────┘    └──────────────┘    └──────────────────┘
            │                                         │
            ▼                                         ▼
    ┌──────────────┐                         ┌──────────────────┐
    │ TilingSprite │                         │  MeshSimple      │
    │NineSliceSprite                         │  MeshPlane       │
    └──────────────┘                         │  MeshRope        │
                                             └──────────────────┘
```

### 3.2 Renderer 계층

```
    ┌────────────────────────────────────────────────────────────┐
    │                    AbstractRenderer                        │
    │  - 기본 렌더링 인터페이스                                   │
    │  - 시스템 관리 (runners)                                    │
    │  - 렌더 파이프 (renderPipes)                                │
    └────────────────────────────────────────────────────────────┘
                                 │
            ┌────────────────────┴────────────────────┐
            ▼                                         ▼
    ┌──────────────────────┐               ┌──────────────────────┐
    │    WebGLRenderer     │               │    WebGPURenderer    │
    │  - GlContextSystem   │               │  - GpuDeviceSystem   │
    │  - GlBufferSystem    │               │  - GpuBufferSystem   │
    │  - GlTextureSystem   │               │  - GpuTextureSystem  │
    │  - GlShaderSystem    │               │  - PipelineSystem    │
    │  - GlStateSystem     │               │  - BindGroupSystem   │
    └──────────────────────┘               └──────────────────────┘
```

### 3.3 Texture 계층

```
    ┌────────────────────────────────────────────────────────────┐
    │                        Texture                             │
    │  - frame, orig, trim (Rectangle)                          │
    │  - rotate (그룹 D8 회전)                                   │
    │  - uvs 관리                                                │
    │  - defaultAnchor                                           │
    └────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
    ┌────────────────────────────────────────────────────────────┐
    │                     TextureSource                          │
    │  - 실제 이미지/비디오/캔버스 데이터                         │
    │  - width, height                                           │
    │  - GPU 업로드 관리                                         │
    └────────────────────────────────────────────────────────────┘
                                 │
            ┌────────────────────┼────────────────────┐
            ▼                    ▼                    ▼
    ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
    │ ImageSource      │ │ VideoSource      │ │ CanvasSource     │
    │ BufferImageSource│ │                  │ │                  │
    └──────────────────┘ └──────────────────┘ └──────────────────┘
```

---

## 4. 주요 클래스별 상세 구조

### 4.1 Application

```typescript
// 파일: src/app/Application.ts

export class Application<R extends Renderer = Renderer> {
    // ═══════════════════════════════════════════════════════
    // 주요 속성
    // ═══════════════════════════════════════════════════════
    stage: Container;        // 루트 디스플레이 컨테이너
    renderer: R;             // 렌더러 인스턴스

    // ═══════════════════════════════════════════════════════
    // 생성자
    // ═══════════════════════════════════════════════════════
    constructor();
    constructor(options?: Partial<ApplicationOptions>);  // deprecated v8.0.0

    // ═══════════════════════════════════════════════════════
    // 주요 메서드
    // ═══════════════════════════════════════════════════════
    async init(options?: Partial<ApplicationOptions>): Promise<void>;
    render(): void;
    resize(width: number, height: number): void;
    destroy(removeView?: boolean, options?: RendererDestroyOptions): void;
}
```

**ApplicationOptions 주요 속성:**

| 속성 | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `width` | `number` | 800 | 캔버스 너비 |
| `height` | `number` | 600 | 캔버스 높이 |
| `backgroundColor` | `ColorSource` | 0x000000 | 배경색 |
| `backgroundAlpha` | `number` | 1 | 배경 투명도 |
| `antialias` | `boolean` | false | 안티앨리어싱 |
| `resolution` | `number` | window.devicePixelRatio | 해상도 |
| `preference` | `'webgl' \| 'webgpu'` | - | 렌더러 타입 |
| `resizeTo` | `Window \| HTMLElement` | - | 자동 리사이징 대상 |
| `autoDensity` | `boolean` | false | 자동 밀도 조절 |
| `autoStart` | `boolean` | true | 자동 렌더링 시작 |
| `sharedTicker` | `boolean` | false | 공유 티커 사용 |

### 4.2 Container

```typescript
// 파일: src/scene/container/Container.ts

export class Container<C extends ContainerChild = ContainerChild>
    extends EventEmitter<ContainerEvents<C> & AnyEvent>
{
    // ═══════════════════════════════════════════════════════
    // Transform 속성
    // ═══════════════════════════════════════════════════════
    position: ObservablePoint;     // {x, y}
    scale: ObservablePoint;        // {x, y}
    pivot: ObservablePoint;        // 회전/스케일 중심점
    skew: ObservablePoint;         // 왜곡
    rotation: number;              // 라디안 단위
    angle: number;                 // 도 단위

    // ═══════════════════════════════════════════════════════
    // 렌더링 속성
    // ═══════════════════════════════════════════════════════
    alpha: number;                 // 투명도 (0-1)
    visible: boolean;              // 가시성
    renderable: boolean;           // 렌더 가능 여부
    tint: number;                  // 색상 틴트
    blendMode: BLEND_MODES;        // 블렌드 모드

    // ═══════════════════════════════════════════════════════
    // 계층 구조
    // ═══════════════════════════════════════════════════════
    readonly parent: Container | null;
    readonly children: C[];

    // ═══════════════════════════════════════════════════════
    // 생성자
    // ═══════════════════════════════════════════════════════
    constructor(options?: ContainerOptions<C>);

    // ═══════════════════════════════════════════════════════
    // 자식 관리 메서드
    // ═══════════════════════════════════════════════════════
    addChild<U extends C[]>(...children: U): U[0];
    addChildAt<U extends C>(child: U, index: number): U;
    removeChild<U extends C[]>(...children: U): U[0];
    removeChildAt(index: number): C;
    removeChildren(beginIndex?: number, endIndex?: number): C[];
    swapChildren(child: C, child2: C): void;
    getChildAt(index: number): C;
    getChildIndex(child: C): number;
    setChildIndex(child: C, index: number): void;

    // ═══════════════════════════════════════════════════════
    // 변환 메서드
    // ═══════════════════════════════════════════════════════
    toGlobal(position: PointData, point?: Point): Point;
    toLocal(position: PointData, from?: Container, point?: Point): Point;

    // ═══════════════════════════════════════════════════════
    // 바운드 메서드
    // ═══════════════════════════════════════════════════════
    getBounds(skipUpdate?: boolean, rect?: Rectangle): Rectangle;
    getLocalBounds(rect?: Rectangle): Rectangle;
}
```

**ContainerOptions 주요 속성:**

| 속성 | 타입 | 설명 |
|------|------|------|
| `isRenderGroup` | `boolean` | 렌더 그룹 여부 |
| `blendMode` | `BLEND_MODES` | 혼합 모드 |
| `tint` | `ColorSource` | 색상 틴트 |
| `alpha` | `number` | 투명도 |
| `visible` | `boolean` | 가시성 |
| `position` | `PointData` | 위치 |
| `x`, `y` | `number` | X, Y 좌표 |
| `rotation` | `number` | 회전 (라디안) |
| `angle` | `number` | 회전 (도) |
| `scale` | `PointData \| number` | 스케일 |
| `pivot` | `PointData \| number` | 피벗 |
| `origin` | `PointData \| number` | 원점 |
| `skew` | `PointData` | 왜곡 |
| `children` | `C[]` | 초기 자식들 |
| `parent` | `Container` | 부모 컨테이너 |

### 4.3 Sprite

```typescript
// 파일: src/scene/sprite/Sprite.ts

export class Sprite extends ViewContainer<BatchableSprite> {
    // ═══════════════════════════════════════════════════════
    // 주요 속성
    // ═══════════════════════════════════════════════════════
    texture: Texture;              // 스프라이트 텍스처
    anchor: ObservablePoint;       // 앵커 포인트 (0-1)
    roundPixels: boolean;          // 픽셀 반올림

    // ═══════════════════════════════════════════════════════
    // 생성자
    // ═══════════════════════════════════════════════════════
    constructor(options?: SpriteOptions | Texture);

    // ═══════════════════════════════════════════════════════
    // 정적 메서드
    // ═══════════════════════════════════════════════════════
    static from(source: TextureSourceLike, skipCache?: boolean): Sprite;

    // ═══════════════════════════════════════════════════════
    // 크기 관련
    // ═══════════════════════════════════════════════════════
    width: number;   // getter/setter
    height: number;  // getter/setter
    getSize(size?: Size): Size;
    setSize(width: number, height: number): void;
}
```

**SpriteOptions 주요 속성:**

| 속성 | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `texture` | `Texture` | `Texture.EMPTY` | 스프라이트 텍스처 |
| `anchor` | `PointData \| number` | 0 | 앵커 포인트 |
| `roundPixels` | `boolean` | false | 픽셀 반올림 |
| + ViewContainerOptions의 모든 속성 | | | |

### 4.4 Graphics

```typescript
// 파일: src/scene/graphics/shared/Graphics.ts

export class Graphics extends ViewContainer<GraphicsGpuData> {
    // ═══════════════════════════════════════════════════════
    // 주요 속성
    // ═══════════════════════════════════════════════════════
    context: GraphicsContext;      // 그리기 컨텍스트
    fillStyle: FillStyle;          // 채우기 스타일
    strokeStyle: StrokeStyle;      // 선 스타일

    // ═══════════════════════════════════════════════════════
    // 생성자
    // ═══════════════════════════════════════════════════════
    constructor(options?: GraphicsOptions | GraphicsContext);

    // ═══════════════════════════════════════════════════════
    // 도형 그리기 메서드
    // ═══════════════════════════════════════════════════════
    rect(x: number, y: number, w: number, h: number): this;
    roundRect(x: number, y: number, w: number, h: number, radius?: number): this;
    circle(x: number, y: number, radius: number): this;
    ellipse(x: number, y: number, radiusX: number, radiusY: number): this;
    poly(points: number[] | PointData[], close?: boolean): this;
    regularPoly(x: number, y: number, radius: number, sides: number, rotation?: number): this;
    star(x: number, y: number, points: number, radius: number, innerRadius?: number): this;

    // ═══════════════════════════════════════════════════════
    // 경로 그리기 메서드
    // ═══════════════════════════════════════════════════════
    moveTo(x: number, y: number): this;
    lineTo(x: number, y: number): this;
    arc(x: number, y: number, radius: number, startAngle: number, endAngle: number): this;
    arcTo(x1: number, y1: number, x2: number, y2: number, radius: number): this;
    bezierCurveTo(cp1x: number, cp1y: number, cp2x: number, cp2y: number, x: number, y: number): this;
    quadraticCurveTo(cpx: number, cpy: number, x: number, y: number): this;
    closePath(): this;

    // ═══════════════════════════════════════════════════════
    // 채우기/선 메서드
    // ═══════════════════════════════════════════════════════
    fill(style?: FillInput): this;
    stroke(style?: StrokeStyle): this;
    cut(): this;

    // ═══════════════════════════════════════════════════════
    // 변환 메서드
    // ═══════════════════════════════════════════════════════
    setTransform(transform: Matrix): this;
    rotateTransform(angle: number): this;
    scaleTransform(x: number, y?: number): this;
    translateTransform(x: number, y?: number): this;
    resetTransform(): this;

    // ═══════════════════════════════════════════════════════
    // 상태 관리
    // ═══════════════════════════════════════════════════════
    save(): this;
    restore(): this;
    clear(): this;

    // ═══════════════════════════════════════════════════════
    // 기타
    // ═══════════════════════════════════════════════════════
    svg(svg: string): this;
    clone(deep?: boolean): Graphics;
}
```

### 4.5 Texture

```typescript
// 파일: src/rendering/renderers/shared/texture/Texture.ts

export class Texture<TextureSourceType extends TextureSource = TextureSource>
    extends EventEmitter<{ update: Texture, destroy: Texture }>
{
    // ═══════════════════════════════════════════════════════
    // 정적 속성
    // ═══════════════════════════════════════════════════════
    static EMPTY: Texture;         // 빈 텍스처
    static WHITE: Texture;         // 흰색 1x1 텍스처

    // ═══════════════════════════════════════════════════════
    // 인스턴스 속성
    // ═══════════════════════════════════════════════════════
    readonly uid: number;
    source: TextureSourceType;     // 텍스처 소스
    frame: Rectangle;              // 프레임 영역
    orig: Rectangle;               // 원본 영역
    trim: Rectangle;               // 트림 영역
    rotate: number;                // 회전 (groupD8)
    defaultAnchor?: { x: number; y: number };
    uvs: UVs;                      // UV 좌표
    width: number;                 // getter
    height: number;                // getter

    // ═══════════════════════════════════════════════════════
    // 생성자
    // ═══════════════════════════════════════════════════════
    constructor(options?: TextureOptions<TextureSourceType>);

    // ═══════════════════════════════════════════════════════
    // 정적 메서드
    // ═══════════════════════════════════════════════════════
    static from(id: TextureSourceLike, skipCache?: boolean): Texture;

    // ═══════════════════════════════════════════════════════
    // 인스턴스 메서드
    // ═══════════════════════════════════════════════════════
    updateUvs(): void;
    update(): void;
    destroy(destroySource?: boolean): void;
}
```

**TextureOptions 주요 속성:**

| 속성 | 타입 | 설명 |
|------|------|------|
| `source` | `TextureSource` | 텍스처 소스 데이터 |
| `label` | `string` | 디버깅용 라벨 |
| `frame` | `Rectangle` | 표시할 텍스처 영역 |
| `orig` | `Rectangle` | 원본 텍스처 영역 |
| `trim` | `Rectangle` | 트림된 영역 |
| `defaultAnchor` | `{ x, y }` | 기본 앵커 포인트 |
| `defaultBorders` | `TextureBorders` | 9-slice 보더 |
| `rotate` | `number` | 회전 값 |
| `dynamic` | `boolean` | 런타임 수정 여부 |

---

## 5. 옵션 인터페이스 연결 관계

### 5.1 옵션 상속 다이어그램

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Options 상속 구조                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────┐                                          │
│  │ PixiMixins.XXXOptions│  ← 전역 확장 가능한 인터페이스            │
│  └──────────┬───────────┘                                          │
│             │ extends                                               │
│             ▼                                                       │
│  ┌──────────────────────┐                                          │
│  │  ContainerOptions    │  ← 기본 컨테이너 옵션                     │
│  │  - position, scale   │                                          │
│  │  - rotation, alpha   │                                          │
│  │  - visible, tint     │                                          │
│  │  - children, parent  │                                          │
│  └──────────┬───────────┘                                          │
│             │ extends                                               │
│             ▼                                                       │
│  ┌──────────────────────┐                                          │
│  │ ViewContainerOptions │  ← GPU 렌더링 가능 컨테이너               │
│  │  - autoGarbageCollect│                                          │
│  └──────────┬───────────┘                                          │
│             │ extends                                               │
│     ┌───────┴───────┬───────────────────┐                          │
│     ▼               ▼                   ▼                          │
│  ┌────────┐   ┌──────────┐      ┌──────────────┐                   │
│  │Sprite  │   │ Graphics │      │   Mesh       │                   │
│  │Options │   │ Options  │      │  Options     │                   │
│  ├────────┤   ├──────────┤      ├──────────────┤                   │
│  │texture │   │context   │      │geometry      │                   │
│  │anchor  │   │roundPixels│     │texture       │                   │
│  │roundPix│   └──────────┘      └──────────────┘                   │
│  └────────┘                                                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 Application Options 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                   ApplicationOptions 구성                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ApplicationOptions                                                 │
│  ├── extends AutoDetectOptions                                     │
│  │   ├── width: number                                             │
│  │   ├── height: number                                            │
│  │   ├── resolution: number                                        │
│  │   ├── preference: 'webgl' | 'webgpu'                           │
│  │   └── SharedRendererOptions                                     │
│  │       ├── backgroundColor: ColorSource                          │
│  │       ├── backgroundAlpha: number                               │
│  │       ├── antialias: boolean                                    │
│  │       ├── autoDensity: boolean                                  │
│  │       ├── powerPreference: PowerPreference                      │
│  │       └── ...                                                   │
│  │                                                                 │
│  └── extends PixiMixins.ApplicationOptions                        │
│      ├── autoStart?: boolean                                       │
│      ├── resizeTo?: Window | HTMLElement                          │
│      ├── sharedTicker?: boolean                                    │
│      └── (확장 가능)                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. 초기화 흐름

### 6.1 Application 초기화

```typescript
// 1. Application 인스턴스 생성
const app = new Application();

// 2. 비동기 초기화 (필수!)
await app.init({
    width: 800,
    height: 600,
    backgroundColor: 0x1099bb,
    preference: 'webgl'  // 또는 'webgpu'
});

// 내부적으로 일어나는 일:
// ┌───────────────────────────────────────────────────────────────┐
// │ app.init() 내부 흐름                                          │
// ├───────────────────────────────────────────────────────────────┤
// │ 1. autoDetectRenderer() 호출                                  │
// │    └─ preference에 따라 WebGLRenderer 또는 WebGPURenderer 선택│
// │                                                               │
// │ 2. renderer.init(options) 호출                                │
// │    ├─ 캔버스 생성                                             │
// │    ├─ WebGL/WebGPU 컨텍스트 초기화                            │
// │    └─ 렌더링 시스템들 초기화                                  │
// │                                                               │
// │ 3. stage = new Container() 생성                               │
// │    └─ 루트 컨테이너 설정                                      │
// │                                                               │
// │ 4. resizeTo 설정 시 리사이즈 이벤트 바인딩                    │
// │                                                               │
// │ 5. autoStart가 true이면 ticker 시작                           │
// └───────────────────────────────────────────────────────────────┘
```

### 6.2 Sprite 생성 및 추가 흐름

```typescript
// 방법 1: 옵션 객체 사용
const sprite1 = new Sprite({
    texture: Texture.from('bunny.png'),
    position: { x: 100, y: 100 },
    anchor: 0.5,
    scale: 2
});

// 방법 2: Texture 직접 전달
const sprite2 = new Sprite(Texture.from('bunny.png'));

// 방법 3: 정적 from 메서드
const sprite3 = Sprite.from('bunny.png');

// stage에 추가
app.stage.addChild(sprite1);
```

### 6.3 Graphics 사용 패턴

```typescript
const graphics = new Graphics();

// 체이닝 패턴으로 도형 그리기
graphics
    .rect(0, 0, 100, 100)           // 사각형 경로 추가
    .fill({ color: 0xff0000 })      // 빨간색으로 채우기
    .circle(150, 50, 40)            // 원 경로 추가
    .fill({ color: 0x00ff00 })      // 초록색으로 채우기
    .stroke({ width: 2, color: 0x0000ff });  // 파란색 선

app.stage.addChild(graphics);
```

### 6.4 렌더링 루프

```typescript
// 방법 1: Ticker 사용 (권장)
app.ticker.add((ticker) => {
    sprite.rotation += 0.01 * ticker.deltaTime;
});

// 방법 2: 수동 렌더링
function gameLoop() {
    requestAnimationFrame(gameLoop);

    // 게임 로직 업데이트
    sprite.rotation += 0.01;

    // 렌더링
    app.renderer.render(app.stage);
}
gameLoop();
```

---

## 부록: 자주 사용되는 패턴

### A. 에셋 로딩

```typescript
import { Assets } from 'pixi.js';

// 단일 에셋 로드
const texture = await Assets.load('bunny.png');

// 여러 에셋 로드
const textures = await Assets.load(['bunny.png', 'background.png']);

// 번들 로드
Assets.addBundle('gameAssets', {
    bunny: 'bunny.png',
    background: 'background.png'
});
await Assets.loadBundle('gameAssets');
```

### B. 이벤트 처리

```typescript
sprite.eventMode = 'static';  // 또는 'dynamic', 'passive', 'auto', 'none'
sprite.cursor = 'pointer';

sprite.on('pointerdown', (event) => {
    console.log('Clicked!', event.global);
});

sprite.on('pointermove', (event) => {
    // 마우스 이동 처리
});
```

### C. 필터 적용

```typescript
import { BlurFilter, ColorMatrixFilter } from 'pixi.js';

const blurFilter = new BlurFilter({ strength: 8 });
const colorFilter = new ColorMatrixFilter();
colorFilter.greyscale(0.5);

sprite.filters = [blurFilter, colorFilter];
```

---

> 📅 마지막 업데이트: 2025-01-11
> 📦 PixiJS 버전: 8.15.0
